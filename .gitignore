# personal board contents - this is your data, not the app
KanbanOverlay.json
KanbanOverlay.json.bak
KanbanOverlay.json.tmp

# the pre-rename names, so an upgraded board never lands in a commit
OverlayBoard.json
OverlayBoard.json.bak
OverlayBoard.json.tmp

__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

build/
dist/
*.spec
